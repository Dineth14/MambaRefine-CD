# Pretrained Weights

Place local pretrained weights here when an encoder or experiment requires manually provided checkpoints.

MambaVision weights are normally resolved by the wrapped MambaVision dependency. VMamba weights should follow the upstream VMamba instructions.
