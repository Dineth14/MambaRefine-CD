# Datasets

Place datasets here using the clean change detection layout:

```text
datasets/<DATASET_NAME>/
  train/A
  train/B
  train/Mask
  val/A
  val/B
  val/Mask
  test/A
  test/B
  test/Mask
```

Files in `A`, `B`, and `Mask` are matched by filename stem.
