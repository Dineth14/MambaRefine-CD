base: base.yaml

experiment:
  name: adaptive_rf

model:
  variant: tiny
  decoder: adaptive_rf

decoder:
  dilation_rates: [1, 2, 4, 8]

resume:
  enabled: false
  checkpoint_path: null
  strict: true
