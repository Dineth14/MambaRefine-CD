base: base.yaml

experiment:
  name: refinement_decoder

model:
  variant: small          # tiny | tiny2 | small | base | large
  decoder: refinement

decoder:
  channels: 256

resume:
  enabled: false
  checkpoint_path: null
  strict: true
