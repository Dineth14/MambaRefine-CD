# Third Party

Optional external dependencies live here.

For VMamba support, place the official VMamba repository at:

```text
third_party/VMamba
```

Then run:

```bash
python tools/setup_vmamba.py
```
