# MambaRefine-CD — Complete Technical Documentation

> Auto-generated by repository audit. Every value in this document was read
> directly from the source code, configs, and results files.
> Last audited: 2026-04-29

---

## Table of Contents

1. [Repository Overview](#1-repository-overview)
2. [Directory Structure](#2-directory-structure)
3. [Dependencies and Environment](#3-dependencies-and-environment)
4. [Model Architecture](#4-model-architecture)
   - 4.1 [High-Level Pipeline](#41-high-level-pipeline)
   - 4.2 [Backbone](#42-backbone)
   - 4.3 [Input Fusion and Temporal Difference](#43-input-fusion-and-temporal-difference)
   - 4.4 [D-RBI Module](#44-d-rbi-module)
   - 4.5 [ARF Decoder](#45-arf-decoder)
   - 4.6 [Boundary Residual Refinement](#46-boundary-residual-refinement)
   - 4.7 [Full Model Class](#47-full-model-class)
   - 4.8 [Module Dependency Graph](#48-module-dependency-graph)
5. [Loss Functions](#5-loss-functions)
6. [Metrics](#6-metrics)
7. [Datasets](#7-datasets)
   - 7.1 [LEVIR-CD](#71-levir-cd)
   - 7.2 [WHU-CD](#72-whu-cd)
   - 7.3 [DSIFN-CD](#73-dsifn-cd)
   - 7.4 [SECOND](#74-second)
8. [Configuration System](#8-configuration-system)
   - 8.1 [Config Hierarchy](#81-config-hierarchy)
   - 8.2 [Dataset Configs](#82-dataset-configs)
   - 8.3 [Model Configs](#83-model-configs)
   - 8.4 [Experiment Configs](#84-experiment-configs)
   - 8.5 [Ablation Configs](#85-ablation-configs)
9. [Training Pipeline](#9-training-pipeline)
10. [Evaluation and Testing Pipeline](#10-evaluation-and-testing-pipeline)
11. [Inference Pipeline](#11-inference-pipeline)
12. [Ablation Studies](#12-ablation-studies)
13. [Experimental Results](#13-experimental-results)
14. [Tools and Utilities](#14-tools-and-utilities)
15. [Tests](#15-tests)
16. [Known Issues and TODOs](#16-known-issues-and-todos)
17. [Data Flow — End to End](#17-data-flow-end-to-end)
18. [Hyperparameter Reference](#18-hyperparameter-reference)
19. [File-by-File Reference](#19-file-by-file-reference)

---

## 1. Repository Overview

MambaRefine-CD is a PyTorch repository for binary remote-sensing change detection from bi-temporal imagery. The active README states that the repository is focused on DSIFN-CD and WHU-CD, with earlier experimental support for other datasets archived. The active method modules listed in `README.md` and `docs/METHOD.md` are MambaVision backbone, D-RBI, signed temporal difference, ARF decoder, and boundary residual refinement. The current codebase has active train/test/infer scripts, active DSIFN/WHU configs, and archived LEVIR/SECOND material.

**Task:** Binary change detection from bi-temporal remote-sensing imagery.

**Main contribution:** Efficient remote-sensing change detection with MambaVision and region-boundary interaction modeling.

**Current state:**
- Training: active via `scripts/train.py` and `training.pipeline.run_training_pipeline`; local audit environment lacks `torch`.
- Testing/Evaluation: active via `scripts/test.py`; `scripts/evaluate.py` forwards to `scripts/test.py`.
- LEVIR-CD: active support archived; old metrics remain in website assets.
- WHU-CD: active config and README result present.
- DSIFN-CD: active config and README result present.
- SECOND: active support archived; old website placeholder says TBD.

**Repository root:** `/storage2/ChangeDetection/MV/MambaRefine-CD`

---


## 3. Dependencies and Environment

**Python version required:** [NOT FOUND IN REPO]

**Raw requirements:**

```
torch>=2.1
torchvision>=0.16
numpy>=1.23
Pillow>=9.0
PyYAML>=6.0
tqdm>=4.64
matplotlib>=3.6
thop>=0.1.1
timm>=0.9
einops>=0.7
mamba-ssm>=2.0
```

| Package | Version specifier | Role |
|---|---|---|
| `torch` | `torch>=2.1` | Deep learning framework |
| `torchvision` | `torchvision>=0.16` | Image transforms and vision utilities |
| `numpy` | `numpy>=1.23` | Array operations |
| `Pillow` | `Pillow>=9.0` | Image IO |
| `PyYAML` | `PyYAML>=6.0` | YAML config parsing |
| `tqdm` | `tqdm>=4.64` | Progress bars |
| `matplotlib` | `matplotlib>=3.6` | Plotting |
| `thop` | `thop>=0.1.1` | FLOP/parameter profiling |
| `timm` | `timm>=0.9` | Vision model dependency |
| `einops` | `einops>=0.7` | Tensor rearrangement |
| `mamba-ssm` | `mamba-ssm>=2.0` | Mamba dependency |

**Installation command:**
```bash
pip install -r requirements.txt
```

**Raw `.gitignore`:**

```
# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so

# PyTorch
*.pth.tar
*.pth
*.pt
*.ckpt
checkpoints/
external_weights/
archive/**/*.pth
archive/**/*.pt

# Outputs (not committed — too large)
outputs/
outputs/benchmark_runs/
outputs/eval_runs/
outputs/dataset_manifests/
debug/
wandb/
runs/

# Keep outputs skeleton
!outputs/.gitkeep

# Datasets
Datasets/

# Logs
*.log
logs/

# Test caches
.pytest_cache/

# Jupyter
.ipynb_checkpoints/
*.ipynb

# VS Code / IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Env
.env
venv/
env/
```

**⚠ Issues:** dependency versions are lower-bounded rather than exactly pinned; MambaVision is loaded from sibling paths in `src/models/backbone/mambavision_builder.py`.

---

## 4. Model Architecture

### 4.1 High-Level Pipeline

**Input:** `img_a`, `img_b` tensors `[B, 3, H, W]`.

**Output:** `DRBISiameseMambaCD.forward` returns `(change_logits, aux_logits)`.

**Forward pass stages:**
1. `self.encoder(img_a)` and `self.encoder(img_b)` generate multi-scale features.
2. If `difference.enabled`, each feature pair goes through `DifferentialRegionBoundaryInteraction`.
3. D-RBI returns `diff`, `region`, `boundary`.
4. Optional `CRAMLiteBank.apply(region_feats)` runs when `model.cram_lite.enabled` and D-RBI are enabled.
5. `AdaptiveRFDecoder` predicts coarse and final logits.
6. If D-RBI is disabled, decoder uses legacy abs-diff path.

**File that defines the top-level model:** `src/models/cd_model.py`

**Top-level class name:** `DRBISiameseMambaCD`

### 4.2 Backbone

**Class name:** external `MambaVisionFeatureExtractor`; local fallback `SimpleCNNFeatureExtractor`.

**File:** `src/models/backbone/mambavision_builder.py`

**Imported from:** `mvcd.model.MambaVisionFeatureExtractor`

**Architecture family:** MambaVision.

| Alias | Registry name | Channels |
|---|---|---|
| `tiny` | `mamba_vision_T` | `[80, 160, 320, 640]` |
| `tiny2` | `mamba_vision_T2` | `[80, 160, 320, 640]` |
| `small` | `mamba_vision_S` | `[96, 192, 384, 768]` |
| `base` | `mamba_vision_B` | `[128, 256, 512, 1024]` |
| `large` | `mamba_vision_L` | `[196, 392, 784, 1568]` |

**Pre-trained weights:** controlled by `model.pretrained`; loading delegated to external extractor.

**Shared weights:** one `self.encoder` instance is called for both images.

**Frozen layers:** config key `model.freeze_backbone` exists, but active freeze implementation was [NOT FOUND IN REPO].

**Config keys:**
```yaml
model:
  backbone: mambavision
  variant: base
  pretrained: true
```

**⚠ Issues:** hardcoded sibling dependency paths `MambaVision_experiments/src` and `MambaVisionCD`.

### 4.3 Input Fusion and Temporal Difference

**File:** `src/models/modules/differential_region_boundary.py`

**Class/function name:** `DifferentialRegionBoundaryInteraction.forward`

| Term | Expression | Shape | Enabled by switch |
|------|-----------|-------|------------------|
| Temporal features | `[f1n, f2n]` | `[B, 2C, H, W]` | always |
| Absolute difference | `torch.abs(f2n - f1n)` | `[B, C, H, W]` | `difference.use_absdiff` |
| Signed difference | `f2n - f1n` | `[B, C, H, W]` | `difference.use_signed_diff` |
| Product | `self.product_scale * f1n * f2n` | `[B, C, H, W]` | `difference.use_product` |

**Total input channels:** `in_channels * (2 + int(use_absdiff) + int(use_signed_diff) + int(use_product))`.

```python
parts = [f1n, f2n]
if self.use_absdiff:
    parts.append(torch.abs(f2n - f1n))
if self.use_signed_diff:
    parts.append(f2n - f1n)
if self.use_product:
    parts.append(self.product_scale * f1n * f2n)
x = torch.cat(parts, dim=1)
```

### 4.4 D-RBI Module

**Full name:** Differential Region-Boundary Interaction

**File:** `src/models/modules/differential_region_boundary.py`

**Class name:** `DifferentialRegionBoundaryInteraction`

#### 4.4.1 Bottleneck Compression

| Layer | Type | In channels | Out channels | Config key |
|-------|------|------------|-------------|------------|
| Conv1x1 | `nn.Conv2d` | `in_ch_total` | `out_channels` | derived |
| Norm | `GroupNorm` | `out_channels` | `out_channels` | [NOT FOUND IN REPO] |
| Activation | `GELU` | — | — | [NOT FOUND IN REPO] |
| Spatial | `_dw_pw` or `_pw` | `out_channels` | `out_channels` | `difference.use_depthwise` |

#### 4.4.2 Region Gate

`psi_r` is `_gate_mlp(out_channels, gate_hidden_ratio)`.

- `region_gate_min = 0.2`
- `region_gate_max = 0.8`

```text
G_r = region_gate_min + (region_gate_max - region_gate_min) * sigmoid(clamp(psi_r(D), -8, 8))
R = G_r * D
```

#### 4.4.3 Boundary Gate

```python
gx = F.conv2d(x, kx, padding=1, groups=self.groups)
gy = F.conv2d(x, ky, padding=1, groups=self.groups)
grad_mag = torch.sqrt(gx ** 2 + gy ** 2 + 1e-6)
return torch.clamp(grad_mag, 0.0, _SOBEL_MAG_CLAMP)
```

- `boundary_gate_min = 0.0`
- `boundary_gate_max = 0.4`

```text
G_b = boundary_gate_min + (boundary_gate_max - boundary_gate_min) * sigmoid(clamp(psi_b(|grad D|), -8, 8))
B = G_b * D
```

#### 4.4.4 Outputs

```python
result = {"diff": d, "region": region, "boundary": boundary}
if self.return_debug:
    result["region_gate"] = g_r
    result["boundary_gate"] = g_b
return result
```

#### 4.4.5 Config Keys for D-RBI

```yaml
difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  product_scale: 0.25
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4
```

**⚠ Issues:** product term is documented as numerically risky and disabled by default.

### 4.5 ARF Decoder

**Full name:** Adaptive Receptive Field Decoder

**File:** `src/models/decoders/adaptive_rf_decoder.py`

**Class name:** `AdaptiveRFDecoder`

**Output:** `(P_f, P_c)` when boundary residual active; `(P_c, None)` otherwise.

#### 4.5.1 ARF Block

**Class name:** `_AdaptiveDilationBlock`

**Dilation rates:** `dilation_rates` or `[1, 2, 4, 8]`.

```python
branches = torch.stack([b(x) for b in self.branches], dim=1)   # B,n,C,H,W
w = F.softmax(self.attn(x), dim=1).view(B, self.n, 1, 1, 1)
return (w * branches).sum(dim=1)
```

#### 4.5.2 FPN Top-Down Path

Uses all input scales. Upsampling is bilinear with `align_corners=False`. Skip merge is addition followed by smoothing convolution.

#### 4.5.3 Coarse Head

`_ConvBNGELU(out_channels, out_channels // 2, k=3)` then `nn.Conv2d(out_channels // 2, 1, 1)`.

### 4.6 Boundary Residual Refinement

**Class name:** `_BoundaryRefineHead`

```python
edge  = self.sobel(torch.sigmoid(P_c))
x     = torch.cat([bnd_feat, P_c, edge], dim=1)
return self.body(x)
```

**Zero initialisation:**

```python
nn.init.zeros_(self.body[-1].weight)
nn.init.zeros_(self.body[-1].bias)
```

**Final prediction:** `P_f = P_c + residual_scale * torch.tanh(delta)` with active `decoder.residual_scale: 0.1`.

### 4.7 Full Model Class

**File:** `src/models/cd_model.py`

**Class name:** `DRBISiameseMambaCD`

**Parent class:** `nn.Module`

**`__init__` arguments:** `__init__(self, cfg: dict) -> None`

**`forward()` signature:**
```python
def forward(self, img_a: torch.Tensor, img_b: torch.Tensor, **_) -> Any
```

**Model variants defined:** Light and Full active; Base archived.

| Variant | Config file | Key differences |
|---|---|---|
| Light | `configs/models/mambarefinecd_light.yaml` | `variant: tiny`, `channels: 128`, `batch_size: 16` |
| Base | `configs/models/mambarefinecd_base.yaml` | [NOT FOUND IN REPO] active; archived under `archive/old_configs/` |
| Full | `configs/models/mambarefinecd_full.yaml` | `variant: base`, `channels: 256`, `batch_size: 8` |

**Parameter count command output:**

```
Traceback (most recent call last):
  File "/storage2/ChangeDetection/MV/MambaRefine-CD/scripts/count_params_flops.py", line 23, in <module>
    import torch
ModuleNotFoundError: No module named 'torch'
```

### 4.8 Module Dependency Graph

```
scripts/train.py
  ├── utils.config.load_config
  ├── utils.ablation.log_parameter_breakdown, log_startup_config
  └── training.pipeline.run_training_pipeline
        ├── data.dataset_builder.build_dataloaders
        │     ├── data.dsifncd.DSIFNCDDataset
        │     └── data.whucd.WHUCDDataset
        ├── models.mambarefinecd.build_model
        │     └── src/models/cd_model.py::DRBISiameseMambaCD
        │           ├── src/models/backbone/mambavision_builder.py::build
        │           │     └── mvcd.model.MambaVisionFeatureExtractor
        │           ├── src/models/modules/differential_region_boundary.py::DifferentialRegionBoundaryInteraction
        │           ├── src/models/modules/cram_lite.py::CRAMLiteBank
        │           └── src/models/decoders/adaptive_rf_decoder.py::AdaptiveRFDecoder
        │                 ├── _AdaptiveDilationBlock
        │                 ├── _BoundaryRefineHead
        │                 └── _SobelEdge
        ├── training.losses.build_loss
        ├── training.trainer.Trainer
        └── training.evaluator.Evaluator
```

---

## 5. Loss Functions

### 5.1 Loss Class Index

| Class name | File | Parent class | `__init__` signature |
|---|---|---|---|
| `DiceLoss` | `losses/binary_losses.py:50` | `nn.Module` | `__init__(self, smooth: float=1.0)` |
| `BCEDiceLoss` | `losses/binary_losses.py:82` | `nn.Module` | `__init__(self, bce_weight: float=1.0, dice_weight: float=1.0)` |
| `FocalLoss` | `losses/binary_losses.py:117` | `nn.Module` | `__init__(self, gamma: float=2.0)` |
| `FocalDiceLoss` | `losses/binary_losses.py:142` | `nn.Module` | `__init__(self, focal_weight: float=1.0, dice_weight: float=1.0, gamma: float=2.0)` |
| `BoundaryLoss` | `losses/boundary_loss.py:81` | `nn.Module` | `__init__(self, loss_type: str='bce_dice', target_type: str='sobel', edge_width: int=3)` |
| `DiceLoss` | `src/training/losses.py:61` | `nn.Module` | `__init__(self, smooth: float=1.0)` |
| `FocalLoss` | `src/training/losses.py:84` | `nn.Module` | `__init__(self, gamma: float=2.0, reduction: str='mean')` |
| `BCEDiceLoss` | `src/training/losses.py:114` | `nn.Module` | `__init__(self, bce_w: float=1.0, dice_w: float=1.0, boundary_w: float=0.0)` |
| `DiceFocalLoss` | `src/training/losses.py:153` | `nn.Module` | `__init__(self, dice_w: float=1.0, focal_w: float=0.3, focal_gamma: float=1.5)` |

### 5.2 Per-Loss Detail

#### `BCEDiceLoss`

**File:** `src/training/losses.py`

**Formula:** `total = bce_w * bce + dice_w * dice + boundary_w * boundary`.

**Arguments to `__init__`:** `__init__(self, bce_w: float = 1.0, dice_w: float = 1.0, boundary_w: float = 0.0) -> None`

**Configurable parameters:** `loss.bce_weight`, `loss.dice_weight`, `loss.boundary_weight`, and nested `loss.final.*`, `loss.boundary.*` aliases.

**`forward()` inputs and outputs:** logits, targets, optional `valid_mask`; returns `(total, bce, dice)`.

#### `DiceLoss`

**Formula:** `1 - (2 * intersection + smooth) / (prediction_sum + target_sum + smooth)`.

#### `FocalLoss`

**Formula:** BCE-with-logits multiplied by `(1 - p_t) ** gamma`.

#### `DiceFocalLoss`

**Formula:** `total = dice_w * dice + focal_w * focal`.

### 5.3 Loss Combination

```python
total, primary_loss, dice = self.loss_fn(logits, lb, valid_mask=valid_mask)
if aux is not None:
    aux_total, _, _ = self.loss_fn(outputs["aux_logits"], lb, valid_mask=valid_mask)
    total = total + self.aux_weight * aux_total
```

**Final loss weight λ for auxiliary supervision:** `decoder.aux_weight`, active value `0.4`.

---

## 6. Metrics

### 6.1 Binary Change Detection Metrics

**File:** `metrics/binary_cd_metrics.py`

| Class name | File | Parent class | `__init__` signature |
|---|---|---|---|
| `BinaryMetrics` | `metrics/binary_cd_metrics.py:20` | `[none]` | `__init__(self, threshold: float=0.5)` |
| `StreamingMetrics` | `src/training/metrics.py:26` | `[none]` | `__init__(self, threshold: float=0.5, boundary_dil: int=3)` |
| `BoundaryMetrics` | `src/training/boundary_metrics.py:37` | `[none]` | `__init__(self, boundary_width: int=3, tolerance: int=2, threshold: float=0.5)` |

| Metric | Formula | Notes |
|--------|---------|-------|
| Precision | `TP / (TP + FP)` | changed class |
| Recall | `TP / (TP + FN)` | changed class |
| F1 | `2 * Pre * Rec / (Pre + Rec)` | changed class |
| IoU | `TP / (TP + FP + FN)` | changed class |
| OA | `(TP + TN) / total` | all pixels |

```python
tp = ((pred == 1) & (target == 1)).sum().item()
tn = ((pred == 0) & (target == 0)).sum().item()
fp = ((pred == 1) & (target == 0)).sum().item()
fn = ((pred == 0) & (target == 1)).sum().item()
```

**Threshold:** `0.5` default in `BinaryMetrics` and active eval configs.

### 6.2 SECOND Metrics (if implemented)

**File:** active `[NOT FOUND IN REPO]`; archived at `archive/removed_second/metrics/second_scd_metrics.py`.

**Status:** archived, not active.

---

## 7. Datasets

### 7.1 LEVIR-CD

**Dataset class:** active `[NOT FOUND IN REPO]`; archived under `archive/removed_levir/`.

**Config file:** active `configs/datasets/levir.yaml` `[NOT FOUND IN REPO]`.

**Archived config contents:**

```yaml
# LEVIR-CD Dataset Configuration
dataset:
  name: LEVIR-CD
  root: /storage2/ChangeDetection/MV/Datasets/LEVIRCD        # update to actual path
  train_split: train
  val_split: val
  test_split: test
  split_files:
    train: splits/levir_train.txt
    val: splits/levir_val.txt
  test_dir: test
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.15
  ignore_index: 255
  task_type: binary_change
  mode: binary
  binary: true
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 4

  # Balanced sampling to address change/no-change imbalance
  balance:
    enabled: false
    min_change_ratio: 0.001
    max_nochange_fraction: 0.5
    oversample_change: true
    changed_patch_weight: 2.0

task: binary_cd

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]

checkpoint:
  selection_metric: F1
  mode: max
```

**⚠ Issues:** archived, not active.

### 7.2 WHU-CD

**Dataset class:** `WHUCDDataset`

**File:** `src/data/whucd.py`

**Config file:** `configs/datasets/whu.yaml`

```yaml
# WHU-CD Dataset Configuration
dataset:
  name: WHU-CD
  root: /storage2/ChangeDetection/MV/Datasets/WHU-CD          # update to actual path
  train_split: train
  val_split: val
  test_split: test
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.2
  ignore_index: 255
  task_type: binary_change
  mode: binary
  binary: true
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 4

task: binary_cd

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]

checkpoint:
  selection_metric: F1
  mode: max
```

**Expected structure:** `train/A`, `train/B`, `train/label`; `val/A`, `val/B`, `val/label`; `test/A`, `test/B`, `test/label`, or flat `A/B/label`.

**Return keys:** `image_a`, `image_b`, `label`, `mask`, `id`, `name`.

### 7.3 DSIFN-CD

**Dataset class:** `DSIFNCDDataset`

**File:** `src/data/dsifncd.py`

**Config file:** `configs/datasets/dsifn.yaml`

```yaml
# DSIFN-CD Dataset Configuration
dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN        # update to actual path
  train_split: train
  val_split: val
  test_split: test
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.2
  ignore_index: 255
  task_type: binary_change
  mode: binary
  binary: true
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 4

task: binary_cd

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]

checkpoint:
  selection_metric: F1
  mode: max
```

**Expected structure:** split folders with `t1`, `t2`, `GT`; flat split-file layout; or flat manual split.

**Return keys:** `image_a`, `image_b`, `label`, `mask`, `id`, `name`.

### 7.4 SECOND

**Dataset class:** active `[NOT FOUND IN REPO]`; archived under `archive/removed_second/`.

**Semantic classes:** archived configs use `num_classes: 7`; `ignore_index: 255`.

**Metrics active:** active `[NOT FOUND IN REPO]`; archived SECOND metrics include OA, mIoU, SeK, Fscd.

**Archived config contents:**

```yaml
# SECOND Dataset Configuration (Semantic Change Detection)
dataset:
  name: SECOND
  root: /storage2/ChangeDetection/MV/Datasets/SECOND          # update to actual path
  train_split: train
  val_split: val
  test_split: test
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  debug_stats: false
  val_ratio: 0.2
  ignore_index: 255
  num_classes: 7
  task_type: semantic_change
  mode: semantic
  binary: false
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 4

task: semantic_cd

metrics:
  task: second_scd
  allowed: [OA, mIoU, SeK, Fscd]
  average_mode: global

model:
  name: MambaRefineCD
  variant: a4_full
  output_mode: semantic_change
  enable_semantic_heads: true
  semantic_head:
    enabled: true
    num_classes: 7
    channels: 128
    share_head: true
    dropout: 0.0

loss:
  type: second_semantic_cd
  second:
    sem_ce_weight: 1.0
    sem_dice_weight: 0.5
    change_weight: 0.5
    consistency_weight: 0.1
    ignore_index: 255

train:
  ema:
    enabled: true
    decay: 0.999
  save_best_by: Fscd

eval:
  use_ema: true
  save_predictions: true
  save_visualizations: true

checkpoint:
  selection_metric: Fscd
  monitor: Fscd
  mode: max
```

### Dataset Class Index

| Class name | File | Parent class | `__init__` signature |
|---|---|---|---|
| `DSIFNCDDataset` | `src/data/dsifncd.py:101` | `Dataset` | `__init__(self, root: str | Path, split: str='train', image_size: int=256, val_ratio: float=0.2, seed: int=42, augment: bool=True, augmentation_ops: Optional[List[str]]=None, a_candidates: Optional[List[str]]=None, b_candidates: Optional[List[str]]=None, label_candidates: Optional[List[str]]=None)` |
| `WHUCDDataset` | `src/data/whucd.py:69` | `Dataset` | `__init__(self, root: str | Path, split: str='train', image_size: int=256, val_ratio: float=0.2, seed: int=42, augment: bool=True, a_candidates: Optional[List[str]]=None, b_candidates: Optional[List[str]]=None, label_candidates: Optional[List[str]]=None)` |

---

## 8. Configuration System

### 8.1 Config Hierarchy

`src/utils/config.py` implements custom YAML loading. `load_config(path)` merges `configs/global_config.yaml` with a requested config unless the requested config is the global config itself, then `_normalize_config` fills defaults and `_apply_explicit_model_module_flags` maps publication-facing model flags to runtime keys.

```python
cfg = load_config(str(config_path))
```

### 8.2 Dataset Configs

#### `configs/datasets/levir.yaml`
[NOT FOUND IN REPO]

#### `configs/datasets/whu.yaml`
```yaml
# WHU-CD Dataset Configuration
dataset:
  name: WHU-CD
  root: /storage2/ChangeDetection/MV/Datasets/WHU-CD          # update to actual path
  train_split: train
  val_split: val
  test_split: test
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.2
  ignore_index: 255
  task_type: binary_change
  mode: binary
  binary: true
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 4

task: binary_cd

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]

checkpoint:
  selection_metric: F1
  mode: max
```

#### `configs/datasets/dsifn.yaml`
```yaml
# DSIFN-CD Dataset Configuration
dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN        # update to actual path
  train_split: train
  val_split: val
  test_split: test
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.2
  ignore_index: 255
  task_type: binary_change
  mode: binary
  binary: true
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 4

task: binary_cd

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]

checkpoint:
  selection_metric: F1
  mode: max
```

#### `configs/datasets/second.yaml`
[NOT FOUND IN REPO]

### 8.3 Model Configs

#### `configs/models/mambarefinecd_light.yaml`
```yaml
# MambaRefineCD Light Model Configuration
# Smaller channels and efficient settings for low-GPU-memory environments.

model:
  mode: dual
  backbone: mambavision
  variant: tiny              # lighter backbone
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  semantic_head_type: lightweight
  semantic_num_classes: 7
  num_classes: 1
  return_features: false
  freeze_backbone: false
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1]     # fewer stages for efficiency
    attention_type: spatial

difference:
  enabled: true
  out_channels: 128          # narrower FPN channels
  use_depthwise: true
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  product_scale: 0.25
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4
  gate_hidden_ratio: 0.25

decoder:
  channels: 128              # narrower decoder
  dilation_rates: [1, 2, 4]  # one fewer dilation rate
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true

hardware:
  device: cuda
  mixed_precision: true

training:
  max_iterations: 40000
  batch_size: 16             # can fit larger batch with lighter model
  lr: 1.0e-4
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2000
  validate_every: 4000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3
```

#### `configs/models/mambarefinecd_base.yaml`
[NOT FOUND IN REPO]

#### `configs/models/mambarefinecd_full.yaml`
```yaml
# MambaRefineCD Full Model Configuration
# All modules enabled: signed diff, CRAM-lite, ARF-FPN, boundary refinement,
# boundary loss.

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  semantic_head_type: lightweight
  semantic_num_classes: 7
  num_classes: 1
  return_features: false
  freeze_backbone: false
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1, 2]
    attention_type: spatial

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  use_absdiff: true
  use_signed_diff: true        # <── full: signed diff enabled
  use_product: false
  product_scale: 0.25
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4
  gate_hidden_ratio: 0.25

decoder:
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true

hardware:
  device: cuda
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3
```

### 8.4 Experiment Configs

#### `configs/experiments/levir_full.yaml`
[NOT FOUND IN REPO]

#### `configs/experiments/whu_full.yaml`
```yaml
experiment:
  name: whu_full
  output_root: outputs/whu/full
  seed: 42

dataset:
  name: WHU-CD
  root: /storage2/ChangeDetection/MV/Datasets/WHU-CD
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1, 2]
    attention_type: spatial
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: true

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  product_scale: 0.25
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true
  aux_weight: 0.4

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01
  skip_nan_steps: true
  nan_diag_every: 50

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25
  log_mask_debug: true
  save_debug_outputs: false

ema:
  enabled: true
  decay: 0.999

resume:
  enabled: false
  checkpoint_path: null
  strict: true

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

#### `configs/experiments/dsifn_full.yaml`
```yaml
experiment:
  name: dsifn_full
  output_root: outputs/dsifn/full
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.2
  image_a_dir_candidates: [t1, T1, A, imageA, before, img1]
  image_b_dir_candidates: [t2, T2, B, imageB, after, img2]
  label_dir_candidates: [mask, GT, label, labels, OUT, change_map]

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1, 2]
    attention_type: spatial
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: true

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  product_scale: 0.25
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true
  aux_weight: 0.4

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01
  skip_nan_steps: true
  nan_diag_every: 50

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25
  log_mask_debug: true
  save_debug_outputs: false
  debug_output_root: debug/dsifn
  debug_max_samples: 30

ema:
  enabled: true
  decay: 0.999

resume:
  enabled: false
  checkpoint_path: null
  strict: true

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

#### `configs/experiments/second_full.yaml`
[NOT FOUND IN REPO]

**Key training hyperparameters:**

| File | dataset.name | experiment.name | model.backbone | model.variant | model.decoder | training.batch_size | training.lr | training.optimizer | training.max_iterations | loss.type | metrics.allowed |
|---|---|---|---|---|---|---:|---:|---|---:|---|---|
| `configs/experiments/dsifn_full.yaml` | `dsifn_full` | `dsifn_full` | `mambavision` | `base` | `adaptive_rf` | `8` | `5.0e-5` | `adamw` | `50000` | `adaptive_rf` | `[Pre, Rec, F1, IoU, OA]` |
| `configs/experiments/whu_full.yaml` | `whu_full` | `whu_full` | `mambavision` | `base` | `adaptive_rf` | `8` | `5.0e-5` | `adamw` | `50000` | `adaptive_rf` | `[Pre, Rec, F1, IoU, OA]` |

### 8.5 Ablation Configs

Active DSIFN ablations:

| ID | Config file | backbone | drbi.enabled | signed_diff.enabled | cram_lite.enabled | arf_fpn.enabled | boundary_refine.enabled | loss.boundary.enabled |
|---|---|---|---:|---:|---:|---:|---:|---:|
| A0 | `configs/ablations/dsifn/a0_fpn_baseline.yaml` | `simple_cnn` | `false` | `false` | `false` | `false` | `false` | `false` |
| A1 | `configs/ablations/dsifn/a1_mambavision_fpn.yaml` | `mambavision` | `false` | `false` | `false` | `false` | `false` | `false` |
| A2 | `configs/ablations/dsifn/a2_mambavision_drbi.yaml` | `mambavision` | `true` | `false` | `false` | `false` | `false` | `false` |
| A3 | `configs/ablations/dsifn/a3_mambavision_drbi_signed.yaml` | `mambavision` | `true` | `true` | `false` | `false` | `false` | `false` |
| A4 | `configs/ablations/dsifn/a4_mambavision_drbi_arf.yaml` | `mambavision` | `true` | `true` | `false` | `true` | `false` | `false` |
| A5 | `configs/ablations/dsifn/a5_mambavision_drbi_arf_boundary.yaml` | `mambavision` | `true` | `true` | `false` | `true` | `true` | `false` |
| A6 | `configs/ablations/dsifn/a6_full.yaml` | `mambavision` | `true` | `true` | `true` | `true` | `true` | `true` |

#### `configs/ablations/dsifn/a0_fpn_baseline.yaml`

```yaml
experiment:
  name: a0_fpn_baseline
  output_root: outputs/dsifn/a0_fpn_baseline
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: simple_cnn
  baseline_channels: [64, 128, 256, 512]
  variant: baseline
  decoder: baseline
  pretrained: false
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: false
  signed_diff:
    enabled: false
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: false
  boundary_refine:
    enabled: false

difference:
  enabled: false
  out_channels: 256
  use_absdiff: false
  use_signed_diff: false
  use_product: false
  pre_norm: false
  use_region_gate: false
  use_boundary_gate: false

decoder:
  type: baseline
  channels: 256
  dilation_rates: []
  use_boundary_residual: false
  residual_scale: 0.0
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

#### `configs/ablations/dsifn/a1_mambavision_fpn.yaml`

```yaml
experiment:
  name: a1_mambavision_fpn
  output_root: outputs/dsifn/a1_mambavision_fpn
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: baseline
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: false
  signed_diff:
    enabled: false
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: false
  boundary_refine:
    enabled: false

difference:
  enabled: false
  out_channels: 256
  use_absdiff: false
  use_signed_diff: false
  use_product: false
  pre_norm: false
  use_region_gate: false
  use_boundary_gate: false

decoder:
  type: baseline
  channels: 256
  dilation_rates: []
  use_boundary_residual: false
  residual_scale: 0.0
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [1]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

#### `configs/ablations/dsifn/a2_mambavision_drbi.yaml`

```yaml
experiment:
  name: a2_mambavision_drbi
  output_root: outputs/dsifn/a2_mambavision_drbi
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: baseline
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: false
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: false
  boundary_refine:
    enabled: false

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: false
  use_product: false
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: baseline
  channels: 256
  dilation_rates: []
  use_boundary_residual: false
  residual_scale: 0.0
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [1]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

#### `configs/ablations/dsifn/a3_mambavision_drbi_signed.yaml`

```yaml
experiment:
  name: a3_mambavision_drbi_signed
  output_root: outputs/dsifn/a3_mambavision_drbi_signed
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: baseline
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: false
  boundary_refine:
    enabled: false

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: baseline
  channels: 256
  dilation_rates: []
  use_boundary_residual: false
  residual_scale: 0.0
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

#### `configs/ablations/dsifn/a4_mambavision_drbi_arf.yaml`

```yaml
experiment:
  name: a4_mambavision_drbi_arf
  output_root: outputs/dsifn/a4_mambavision_drbi_arf
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: false

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: false
  residual_scale: 0.0
  use_depthwise: true
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

#### `configs/ablations/dsifn/a5_mambavision_drbi_arf_boundary.yaml`

```yaml
experiment:
  name: a5_mambavision_drbi_arf_boundary
  output_root: outputs/dsifn/a5_mambavision_drbi_arf_boundary
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: true

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [1]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

#### `configs/ablations/dsifn/a6_full.yaml`

```yaml
experiment:
  name: a6_full
  output_root: outputs/dsifn/a6_full
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1, 2]
    attention_type: spatial
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: true

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true
  aux_weight: 0.4

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

---

## 9. Training Pipeline

**Script:** `scripts/train.py`

**Full content:**

```python
"""Training script for active MambaRefine-CD binary experiments.

Usage:
    python scripts/train.py --config configs/experiments/dsifn_full.yaml
    python scripts/train.py --config configs/experiments/whu_full.yaml --dry_run
"""
from __future__ import annotations

import argparse
import logging
import sys
from pathlib import Path

# Ensure repo root and src are on path
_REPO = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(_REPO / "src"))
sys.path.insert(0, str(_REPO))

from utils.config import load_config
from utils.ablation import log_parameter_breakdown, log_startup_config


logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s  %(levelname)-8s  %(message)s",
    datefmt="%H:%M:%S",
)
logger = logging.getLogger(__name__)


# --------------------------------------------------------------------------
# Metric restrictions
# --------------------------------------------------------------------------
_BINARY_ALLOWED = {"Pre", "Rec", "F1", "IoU", "OA"}
def _get_allowed_metrics(cfg: dict) -> list[str]:
    """Return the set of allowed metric names for this config."""
    explicit = cfg.get("metrics", {}).get("allowed", None)
    if explicit is not None:
        return list(explicit)
    return ["Pre", "Rec", "F1", "IoU", "OA"]


def _filter_metrics(raw: dict, allowed: list[str]) -> dict:
    """Return only the keys in `allowed` from `raw`."""
    return {k: v for k, v in raw.items() if k in allowed}


# --------------------------------------------------------------------------
# Main
# --------------------------------------------------------------------------

def main() -> None:
    parser = argparse.ArgumentParser(description="Train MambaRefineCD.")
    parser.add_argument("--config", required=True, help="Path to training config YAML.")
    parser.add_argument("--dry_run", action="store_true",
                        help="Build model and run one batch; do not train.")
    parser.add_argument("--resume", type=str, default=None,
                        help="Path to checkpoint to resume from.")
    args = parser.parse_args()

    config_path = Path(args.config)
    if not config_path.exists():
        logger.error(f"Config not found: {config_path}")
        sys.exit(1)

    cfg = load_config(str(config_path))
    allowed = _get_allowed_metrics(cfg)

    logger.info(f"Config: {config_path}")
    logger.info(f"Dataset: {cfg.get('dataset', {}).get('name', 'unknown')}")
    logger.info(f"Allowed metrics: {allowed}")
    log_startup_config(logger, cfg, config_path)

    if args.resume:
        cfg.setdefault("resume", {})
        cfg["resume"]["enabled"] = True
        cfg["resume"]["checkpoint_path"] = args.resume

    if args.dry_run:
        logger.info("=== DRY RUN MODE ===")
        _dry_run(cfg, allowed)
        return

    # Run full training pipeline
    from training.pipeline import run_training_pipeline

    # Patch allowed metrics into cfg so the pipeline can filter logs
    cfg.setdefault("metrics", {})["allowed"] = list(allowed)

    run_training_pipeline(cfg, config_source_path=config_path)


def _dry_run(cfg: dict, allowed: list[str]) -> None:
    """Build model + dataset, run one forward pass; verify metric restriction."""
    import torch
    from models.mambarefinecd import build_model
    from data.dataset_builder import build_dataset

    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    logger.info(f"Device: {device}")
    log_startup_config(logger, cfg, cfg.get("_meta", {}).get("config_path"))

    # Build model
    model = build_model(cfg).to(device)
    log_parameter_breakdown(logger, model)
    n_params = sum(p.numel() for p in model.parameters() if p.requires_grad)
    logger.info(f"Model parameters: {n_params / 1e6:.2f}M")
    for label, module_name in [
        ("Backbone params", "encoder"),
        ("Decoder params", "decoder"),
        ("D-RBI params", "diff_modules"),
    ]:
        module = getattr(model, module_name, None)
        count = sum(p.numel() for p in module.parameters()) if module is not None else 0
        logger.info(f"{label}: {count / 1e6:.2f}M")
    binary_head = None
    decoder = getattr(model, "decoder", None)
    if decoder is not None:
        binary_head = getattr(decoder, "head", getattr(decoder, "coarse_head", None))
    binary_count = sum(p.numel() for p in binary_head.parameters()) if binary_head is not None else 0
    logger.info(f"Binary head params: {binary_count / 1e6:.2f}M")

    # Build dataset
    ds = build_dataset(
        cfg.get("dataset", {}),
        split="train",
        augment=True,
        seed=int(cfg.get("experiment", {}).get("seed", 42)),
    )
    logger.info(f"Dataset size (train): {len(ds)}")

    # One forward pass
    sample = ds[0]
    img_a = sample["image_a"].unsqueeze(0).to(device)
    img_b = sample["image_b"].unsqueeze(0).to(device)

    skip_forward = (
        device.type == "cpu"
        and str(cfg.get("model", {}).get("backbone", "")).lower() == "mambavision"
    )
    if skip_forward:
        logger.warning(
            "Skipping MambaVision CPU forward pass: installed selective_scan kernel requires CUDA."
        )
        out = None
    else:
        model.eval()
        with torch.no_grad():
            out = model(img_a, img_b)
        logger.info(f"Forward pass OK. Output type: {type(out)}")

    from metrics.binary_cd_metrics import BinaryMetrics
    m = BinaryMetrics()
    fake_logits = torch.zeros(1, 1, 256, 256)
    fake_gt = torch.zeros(1, 256, 256, dtype=torch.long)
    m.update(fake_logits, fake_gt)
    results = m.compute()
    results_filtered = _filter_metrics(results, allowed)
    logger.info(f"Metric keys (allowed only): {list(results_filtered.keys())}")
    assert set(results_filtered.keys()) <= set(allowed), \
        f"Metric keys {set(results_filtered.keys())} not subset of {allowed}"

    logger.info("=== DRY RUN PASSED ===")
    logger.info(f"Confirmed log will contain only: {allowed}")


if __name__ == "__main__":
    main()
```

**Command-line arguments:**

| Argument | Type | Default | Description |
|----------|------|---------|-------------|
| `--config` | str | required | Path to training config YAML. |
| `--dry_run` | bool | `False` | Build model and run one batch; do not train. |
| `--resume` | str | `None` | Path to checkpoint to resume from. |

**Training loop structure:** config load -> model/data/loss/optimizer/scheduler build in `training.pipeline.run_training_pipeline` -> `Trainer.train` -> `Trainer._step` -> validation via `Evaluator` -> checkpoint via `training.checkpoint.save`.

**Logging backend:** Python logging plus optional TensorBoard `SummaryWriter`.

**Mixed precision:** `torch.amp.autocast` and GradScaler.

**Gradient clipping:** `training.gradient_clip`, active value `0.5`.

**⚠ Issues:** local audit shell has no `torch`.

---

## 10. Evaluation and Testing Pipeline

**Scripts:** `scripts/test.py`, `scripts/evaluate.py`

```python
=== scripts/test.py ===
"""Test script for active DSIFN/WHU binary change-detection experiments.

Usage:
    python scripts/test.py --config configs/experiments/dsifn_full.yaml --ckpt <checkpoint>
"""
from __future__ import annotations

import argparse
import csv
import json
import logging
import sys
from pathlib import Path

_REPO = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(_REPO / "src"))
sys.path.insert(0, str(_REPO))

import torch

from utils.config import load_config
from utils.ablation import compare_checkpoint_config, config_fingerprint, log_parameter_breakdown, log_startup_config, module_flags

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s  %(levelname)-8s  %(message)s",
    datefmt="%H:%M:%S",
)
logger = logging.getLogger(__name__)

_BINARY_ALLOWED = {"Pre", "Rec", "F1", "IoU", "OA"}
def _get_allowed_metrics(cfg: dict) -> list[str]:
    explicit = cfg.get("metrics", {}).get("allowed", None)
    if explicit is not None:
        return list(explicit)
    return ["Pre", "Rec", "F1", "IoU", "OA"]


def _filter_metrics(raw: dict, allowed: list[str]) -> dict:
    return {k: v for k, v in raw.items() if k in allowed}


def _merged_eval_cfg(cfg: dict) -> dict:
    merged = dict(cfg.get("evaluation", {}) or {})
    merged.update(dict(cfg.get("eval", {}) or {}))
    return merged


def _paper_binary_metrics(raw: dict) -> dict:
    return {
        "Pre": round(float(raw.get("precision", raw.get("precision_1", 0.0))) * 100.0, 4),
        "Rec": round(float(raw.get("recall", raw.get("recall_1", 0.0))) * 100.0, 4),
        "F1": round(float(raw.get("f1", raw.get("f1_1", 0.0))) * 100.0, 4),
        "IoU": round(float(raw.get("iou", raw.get("iou_1", 0.0))) * 100.0, 4),
        "OA": round(float(raw.get("oa", 0.0)) * 100.0, 4),
    }


def _resolve_threshold(args, cfg: dict, ckpt: dict) -> tuple[float, str]:
    ec = _merged_eval_cfg(cfg)
    if args.threshold is not None:
        return float(args.threshold), "command-line"
    if ckpt.get("best_threshold") is not None:
        return float(ckpt["best_threshold"]), "checkpoint"
    return float(ec.get("threshold", 0.5)), "config"


def _resolve_use_ema(args, cfg: dict) -> bool:
    if args.use_ema is not None:
        return bool(args.use_ema)
    ec = _merged_eval_cfg(cfg)
    if "use_ema" in ec:
        return bool(ec.get("use_ema"))
    return bool(cfg.get("training", {}).get("use_ema", cfg.get("ema", {}).get("enabled", False)))


def _save_results(metrics: dict, save_dir: Path) -> None:
    save_dir.mkdir(parents=True, exist_ok=True)
    with open(save_dir / "metrics.json", "w") as f:
        json.dump(metrics, f, indent=2)
    with open(save_dir / "metrics.csv", "w", newline="") as f:
        w = csv.writer(f)
        w.writerow(["metric", "value"])
        for k, v in metrics.items():
            w.writerow([k, v])
    logger.info(f"Saved results to {save_dir}")


def main() -> None:
    parser = argparse.ArgumentParser(description="Test MambaRefineCD on held-out test split.")
    parser.add_argument("--config",          required=True)
    parser.add_argument("--ckpt",            required=True)
    parser.add_argument("--split",           default="test", choices=["test"])
    parser.add_argument("--threshold",       type=float, default=None)
    ema_group = parser.add_mutually_exclusive_group()
    ema_group.add_argument("--use_ema", dest="use_ema", action="store_true", default=None)
    ema_group.add_argument("--no_ema", dest="use_ema", action="store_false")
    parser.add_argument("--strict", action="store_true", default=True)
    parser.add_argument("--non_strict", dest="strict", action="store_false")
    parser.add_argument("--save_predictions", action=argparse.BooleanOptionalAction, default=None)
    parser.add_argument("--save_visualizations", action=argparse.BooleanOptionalAction, default=None)
    parser.add_argument("--save_debug", action="store_true")
    parser.add_argument("--num_workers", type=int, default=None)
    args = parser.parse_args()

    cfg     = load_config(args.config)
    if args.num_workers is not None:
        cfg.setdefault("dataset", {})["num_workers"] = int(args.num_workers)
    allowed = _get_allowed_metrics(cfg)
    device  = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    save_predictions = bool(args.save_predictions) if args.save_predictions is not None else False
    save_visualizations = bool(args.save_visualizations) if args.save_visualizations is not None else False

    logger.info(f"Config: {args.config}")
    logger.info(f"Checkpoint: {args.ckpt}")
    logger.info(f"Split: {args.split}")
    logger.info(f"Allowed metrics: {allowed}")
    log_startup_config(logger, cfg, args.config)

    from models.mambarefinecd import build_model
    from data.dataset_builder import build_test_loader
    from training.checkpoint import load_for_eval, peek as peek_ckpt
    from training.evaluator import Evaluator

    ckpt_meta = peek_ckpt(args.ckpt, map_location=device)
    logger.info("Config ablation name: %s", cfg.get("experiment", {}).get("name", "unknown"))
    compare_checkpoint_config(logger, cfg, ckpt_meta, strict=bool(args.strict))
    threshold, threshold_source = _resolve_threshold(args, cfg, ckpt_meta)
    use_ema_requested = _resolve_use_ema(args, cfg)

    cfg.setdefault("evaluation", {})
    cfg["evaluation"]["split"] = args.split
    cfg["evaluation"]["threshold"] = threshold
    cfg["evaluation"]["save_predictions"] = save_predictions
    cfg["evaluation"]["save_visualizations"] = save_visualizations
    cfg.setdefault("eval", {})
    cfg["eval"]["split"] = args.split
    cfg["eval"]["threshold"] = threshold
    cfg["eval"]["save_predictions"] = save_predictions
    cfg["eval"]["save_visualizations"] = save_visualizations
    cfg.setdefault("output", {})
    cfg["output"]["save_predictions"] = save_predictions
    cfg["output"]["save_visualizations"] = save_visualizations
    if args.save_debug:
        dataset_slug = str(cfg.get("dataset", {}).get("name", "dataset")).lower().replace("-cd", "").replace("-", "_")
        cfg["evaluation"]["save_debug_outputs"] = True
        cfg["evaluation"]["debug_output_root"] = f"debug/{dataset_slug}"
        cfg["evaluation"]["debug_max_samples"] = 50
        cfg["eval"]["save_debug_outputs"] = True
        cfg["eval"]["debug_output_root"] = f"debug/{dataset_slug}"
        cfg["eval"]["debug_max_samples"] = 50
    if args.split == "test" or args.threshold is not None or threshold_source == "checkpoint":
        cfg["evaluation"]["threshold_sweep"] = False
        cfg["eval"]["threshold_sweep"] = False

    loader = build_test_loader(cfg)

    model = build_model(cfg).to(device)
    log_parameter_breakdown(logger, model)
    load_info = load_for_eval(
        args.ckpt,
        model,
        map_location=device,
        strict=bool(args.strict),
        use_ema=use_ema_requested,
    )
    logger.info(f"Loaded checkpoint from {args.ckpt}")
    logger.info(f"Checkpoint iteration: {load_info['iteration']} | best_metric: {load_info['best_metric']}")
    logger.info(f"Checkpoint threshold stored: {load_info['best_threshold']}")
    logger.info(f"Using threshold: {threshold:.4f}")
    logger.info(f"Threshold source: {threshold_source}")
    logger.info(f"Using EMA: {str(load_info['ema_used']).lower()}")
    logger.info(f"EMA weights found: {str(load_info['ema_found']).lower()}")
    if use_ema_requested and not load_info["ema_found"]:
        logger.warning("eval.use_ema/--use_ema requested but checkpoint has no EMA weights; using raw model weights.")
    logger.info(f"Missing keys: {load_info['missing_keys']}")
    logger.info(f"Unexpected keys: {load_info['unexpected_keys']}")

    out_root = Path(cfg.get("experiment", {}).get("output_root", "outputs/test"))
    evaluator = Evaluator(cfg, device, logger=logger, save_dir=out_root)
    raw = evaluator.evaluate(
        model,
        loader,
        dataset_name=cfg.get("dataset", {}).get("name", "unknown"),
        amp=bool(cfg.get("hardware", {}).get("mixed_precision", True)),
    )

    effective_threshold = float(raw.get("best_threshold", threshold))
    effective_source = "validation-sweep" if effective_threshold != threshold and args.split == "val" else threshold_source
    results = _paper_binary_metrics(raw)
    results["threshold"] = effective_threshold
    results["threshold_source"] = effective_source
    results["ema_used"] = bool(load_info["ema_used"])
    results["ema_found"] = bool(load_info["ema_found"])
    results["config_fingerprint"] = cfg.get("_meta", {}).get("config_fingerprint", config_fingerprint(cfg))
    results["module_flags"] = module_flags(cfg)

    logger.info("=" * 50)
    logger.info("Test Results:")
    for k in allowed:
        v = results.get(k)
        if v is None:
            continue
        logger.info(f"  {k:16s}: {float(v):.4f}")
    logger.info("-" * 50)
    logger.info("Evaluation metadata:")
    for k in ("threshold", "threshold_source", "ema_used", "ema_found"):
        v = results.get(k)
        if v is None:
            continue
        if isinstance(v, bool):
            logger.info(f"  {k:16s}: {str(v).lower()}")
        elif isinstance(v, (int, float)):
            logger.info(f"  {k:16s}: {v:.4f}")
        else:
            logger.info(f"  {k:16s}: {v}")
    logger.info("=" * 50)

    _save_results(results, out_root)


if __name__ == "__main__":
    main()

=== scripts/evaluate.py ===
"""Compatibility evaluator for active binary CD experiments.

This script forwards to `scripts/test.py` and keeps the older entry point
available for DSIFN-CD and WHU-CD checkpoints.
"""
from __future__ import annotations

import subprocess
import sys
from pathlib import Path


def main() -> None:
    repo = Path(__file__).resolve().parents[1]
    test_script = repo / "scripts" / "test.py"
    cmd = [sys.executable, str(test_script), *sys.argv[1:]]
    raise SystemExit(subprocess.call(cmd))


if __name__ == "__main__":
    main()
```

**Differences:** `test.py` performs checkpoint evaluation; `evaluate.py` is a compatibility wrapper that calls `scripts/test.py` with the same arguments.

**Threshold:** command-line threshold, checkpoint `best_threshold`, or config threshold default `0.5`.

**Metrics computed:** `Pre`, `Rec`, `F1`, `IoU`, `OA`.

---

## 11. Inference Pipeline

**Script:** `scripts/infer.py`

```python
"""Inference script — run change detection on an image pair or a folder.

No metrics are computed if ground truth is not provided.
Outputs change probability maps (PNG) and optionally binary masks.

Usage:
    # Single pair
    python scripts/infer.py --config configs/experiments/dsifn_full.yaml \\
                             --ckpt <checkpoint> \\
                             --img_a /path/to/t1.png --img_b /path/to/t2.png

    # Folder of pairs (expects subfolders A/ and B/ with matching filenames)
    python scripts/infer.py --config configs/experiments/whu_full.yaml \\
                             --ckpt <checkpoint> \\
                             --folder /path/to/pairs/ --out /path/to/output/
"""
from __future__ import annotations

import argparse
import logging
import sys
from pathlib import Path

_REPO = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(_REPO / "src"))
sys.path.insert(0, str(_REPO))

import numpy as np
import torch
import torchvision.transforms.functional as TF
from PIL import Image as PILImage

from utils.config import load_config

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s  %(levelname)-8s  %(message)s",
    datefmt="%H:%M:%S",
)
logger = logging.getLogger(__name__)

_IMG_MEAN = [0.485, 0.456, 0.406]
_IMG_STD  = [0.229, 0.224, 0.225]


def _load_image(path: str | Path) -> torch.Tensor:
    """Load RGB image → normalised float tensor [1, 3, H, W]."""
    img = PILImage.open(path).convert("RGB")
    t   = TF.to_tensor(img)
    t   = TF.normalize(t, _IMG_MEAN, _IMG_STD)
    return t.unsqueeze(0)


def _run_pair(model: torch.nn.Module, path_a: Path, path_b: Path,
              device: torch.device, threshold: float):
    """Run one pair."""
    from training.model_outputs import normalize_model_output

    with torch.no_grad():
        img_a = _load_image(path_a).to(device)
        img_b = _load_image(path_b).to(device)
        out   = model(img_a, img_b)
        outputs = normalize_model_output(out)
        logits = outputs["change_logits"]
        prob = torch.sigmoid(logits[0, 0]).cpu().numpy()

    prob_u8   = (prob * 255).astype(np.uint8)
    binary_u8 = ((prob > threshold) * 255).astype(np.uint8)
    return prob_u8, binary_u8


def main() -> None:
    parser = argparse.ArgumentParser(description="MambaRefineCD inference.")
    parser.add_argument("--config",    required=True)
    parser.add_argument("--ckpt",      required=True)
    parser.add_argument("--threshold", type=float, default=0.5)
    # Single pair
    parser.add_argument("--img_a", type=str, default=None)
    parser.add_argument("--img_b", type=str, default=None)
    # Folder mode
    parser.add_argument("--folder", type=str, default=None,
                        help="Root folder with subdirs A/ and B/ containing matching files.")
    parser.add_argument("--out",    type=str, default=None,
                        help="Output directory for predictions.")
    args = parser.parse_args()

    if args.img_a is None and args.folder is None:
        parser.error("Provide either --img_a / --img_b or --folder.")

    cfg    = load_config(args.config)
    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")

    from models.mambarefinecd import build_model
    from training.checkpoint import load as load_ckpt

    model = build_model(cfg).to(device)
    load_ckpt(args.ckpt, model, map_location=device)
    model.eval()
    logger.info(f"Loaded model from {args.ckpt}")

    out_root = Path(args.out) if args.out else _REPO / "outputs" / "infer"

    if args.img_a is not None:
        # Single pair
        result = _run_pair(model, Path(args.img_a), Path(args.img_b), device, args.threshold)
        out_root.mkdir(parents=True, exist_ok=True)
        stem = Path(args.img_a).stem
        prob, binary = result
        PILImage.fromarray(prob).save(out_root / f"{stem}_prob.png")
        PILImage.fromarray(binary).save(out_root / f"{stem}_binary.png")
        logger.info(f"Saved to {out_root}")
    else:
        # Folder mode
        folder = Path(args.folder)
        a_dir  = folder / "A"
        b_dir  = folder / "B"
        if not a_dir.is_dir() or not b_dir.is_dir():
            logger.error(f"Expected subfolders A/ and B/ under {folder}")
            sys.exit(1)

        prob_dir   = out_root / "prob"
        binary_dir = out_root / "binary"
        prob_dir.mkdir(parents=True, exist_ok=True)
        binary_dir.mkdir(parents=True, exist_ok=True)

        pairs = sorted(a_dir.glob("*"))
        logger.info(f"Found {len(pairs)} pairs")

        for path_a in pairs:
            path_b = b_dir / path_a.name
            if not path_b.exists():
                logger.warning(f"No matching B for {path_a.name}, skipping.")
                continue
            result = _run_pair(model, path_a, path_b, device, args.threshold)
            prob, binary = result
            PILImage.fromarray(prob).save(prob_dir / path_a.name)
            PILImage.fromarray(binary).save(binary_dir / path_a.name)

        logger.info(f"Inference complete. Results in {out_root}")


if __name__ == "__main__":
    main()
```

**Input:** `--img_a`/`--img_b` or `--folder` with `A/` and `B/`.

**Output:** probability PNG and binary PNG.

**Post-processing:** sigmoid and threshold only.

---

## 12. Ablation Studies

### 12.1 What Each Ablation Tests

| ID | Config file | backbone | drbi.enabled | signed_diff.enabled | cram_lite.enabled | arf_fpn.enabled | boundary_refine.enabled | loss.boundary.enabled |
|---|---|---|---:|---:|---:|---:|---:|---:|
| A0 | `configs/ablations/dsifn/a0_fpn_baseline.yaml` | `simple_cnn` | `false` | `false` | `false` | `false` | `false` | `false` |
| A1 | `configs/ablations/dsifn/a1_mambavision_fpn.yaml` | `mambavision` | `false` | `false` | `false` | `false` | `false` | `false` |
| A2 | `configs/ablations/dsifn/a2_mambavision_drbi.yaml` | `mambavision` | `true` | `false` | `false` | `false` | `false` | `false` |
| A3 | `configs/ablations/dsifn/a3_mambavision_drbi_signed.yaml` | `mambavision` | `true` | `true` | `false` | `false` | `false` | `false` |
| A4 | `configs/ablations/dsifn/a4_mambavision_drbi_arf.yaml` | `mambavision` | `true` | `true` | `false` | `true` | `false` | `false` |
| A5 | `configs/ablations/dsifn/a5_mambavision_drbi_arf_boundary.yaml` | `mambavision` | `true` | `true` | `false` | `true` | `true` | `false` |
| A6 | `configs/ablations/dsifn/a6_full.yaml` | `mambavision` | `true` | `true` | `true` | `true` | `true` | `true` |

### 12.2 Exact Config Diffs

**a0_fpn_baseline vs A6:**
```text
backbone: simple_cnn -> mambavision
drbi: false -> true
signed_diff: false -> true
cram_lite: false -> true
arf_fpn: false -> true
boundary_refine: false -> true
boundary_loss: false -> true
```
**a1_mambavision_fpn vs A6:**
```text
drbi: false -> true
signed_diff: false -> true
cram_lite: false -> true
arf_fpn: false -> true
boundary_refine: false -> true
boundary_loss: false -> true
```
**a2_mambavision_drbi vs A6:**
```text
signed_diff: false -> true
cram_lite: false -> true
arf_fpn: false -> true
boundary_refine: false -> true
boundary_loss: false -> true
```
**a3_mambavision_drbi_signed vs A6:**
```text
cram_lite: false -> true
arf_fpn: false -> true
boundary_refine: false -> true
boundary_loss: false -> true
```
**a4_mambavision_drbi_arf vs A6:**
```text
cram_lite: false -> true
boundary_refine: false -> true
boundary_loss: false -> true
```
**a5_mambavision_drbi_arf_boundary vs A6:**
```text
cram_lite: false -> true
boundary_loss: false -> true
```

### 12.3 Ablation Results

Requested ablations JSON command output:

```json
[NO OUTPUT]
```

`website/assets/tables/ablation_summary.csv`:

```csv
dataset,experiment,method,F1_1,IoU_1,OA,Precision_1,Recall_1,Boundary_F1,best_threshold,delta_F1,eval_file
```

| ID | Pre | Rec | F1 | IoU | OA | Status |
|----|-----|-----|----|----|-----|--------|
| A0 | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | TODO |
| A1 | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | TODO |
| A2 | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | TODO |
| A3 | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | TODO |
| A4 | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | TODO |
| A5 | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | TODO |
| A6 | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | TODO |

---

## 13. Experimental Results

### 13.1 Verified Results

Requested `main_results.json` command output:

```json
[NO OUTPUT]
```

`website/assets/results/verified_results.json`:

```json
{
  "verified_results": {
    "LEVIR-CD": {
      "mF1": 0.9258,
      "F1_1": 0.8576,
      "mIoU": 0.8693,
      "IoU_1": 0.7507,
      "Precision_1": 0.8515,
      "Recall_1": 0.8638,
      "OA": 0.9883,
      "Boundary_F1": 0.8632,
      "Edge_IoU": 0.5899
    },
    "WHU-CD": {
      "mF1": 0.9723,
      "F1_1": 0.9472,
      "mIoU": 0.9472,
      "IoU_1": 0.8996,
      "Precision_1": 0.9495,
      "Recall_1": 0.9449,
      "OA": 0.9949,
      "Boundary_F1": 0.8966,
      "Edge_IoU": 0.6533
    }
  },
  "placeholders": {
    "DSIFN-CD": {
      "status": "TBD from verified run logs"
    },
    "SECOND": {
      "status": "TBD from verified evaluation",
      "note": "Confirm binary vs semantic protocol before publishing numbers"
    },
    "efficiency": {
      "params": "43.52M",
      "flops": "TBD",
      "gpu_memory": "TBD",
      "fps": "TBD"
    }
  }
}
```

README result rows:

```
| DSIFN-CD | 96.86 | 97.20 | 97.03 | 94.23 | 97.93 |
| WHU-CD | 96.16 | 95.00 | 95.58 | 91.53 | 99.58 |
```

| Dataset | Pre | Rec | F1 | IoU | OA | Note |
|---------|-----|-----|----|----|-----|------|
| DSIFN-CD | 96.86 | 97.20 | 97.03 | 94.23 | 97.93 | from `README.md` |
| WHU-CD | 96.16 | 95.00 | 95.58 | 91.53 | 99.58 | from `README.md` |

### 13.2 Result Source Tracing

Config/checkpoint/split/evaluation script for README DSIFN/WHU numbers: [NOT FOUND IN REPO].

### 13.3 SECOND Results

Status: not active; placeholder in website JSON says `TBD from verified evaluation`.


Result txt files:
```
[NO OUTPUT]
```

---

## 14. Tools and Utilities

### `tools/check_model_params.py`

**Purpose:** Check active DSIFN-CD ablation parameter counts are strictly increasing.

**Definitions:** Functions: `main`

**Arguments:** inspect `argparse` definitions in file; config keys referenced: `arf_fpn_enabled`, `boundary_loss_enabled`, `boundary_refine_enabled`, `cram_lite_enabled`, `drbi_enabled`, `encoder_name`, `pretrained`, `signed_diff_enabled`, `total_params`, `trainable_params`

**⚠ Issues:** [none]
### `tools/export_results_table.py`

**Purpose:** Export experiment results to a formatted table.

**Definitions:** Functions: `_load_record`, `_to_row`, `_markdown_table`, `main`, `_write`

**Arguments:** inspect `argparse` definitions in file; config keys referenced: `---`, `FLOPs(G)`, `Params(M)`, `dataset`, `experiment`, `flops_G`, `params_M`

**⚠ Issues:** [none]
### `tools/validate_dataset.py`

**Purpose:** Dataset validation tool.

**Definitions:** Functions: `_validate_config`, `main`

**Arguments:** inspect `argparse` definitions in file; config keys referenced: `dataset`, `image_a`, `image_b`, `image_size`, `mask`

**⚠ Issues:** [none]
### `tools/verify_ablation_configs.py`

**Purpose:** Verify active DSIFN ablation configs instantiate different models.

**Definitions:** Functions: `_shape_of_output`, `verify_one`, `write_report`, `main`

**Arguments:** inspect `argparse` definitions in file; config keys referenced: `a0_fpn_baseline`, `a6_full`, `arf_fpn_enabled`, `arf_params`, `boundary_loss_enabled`, `boundary_refine_enabled`, `boundary_refinement_params`, `change_logits`, `config`, `cram_lite_enabled`, `cram_lite_params`, `decoder_name`, `decoder_params`, `drbi_enabled`, `drbi_params`, `encoder_name`, `encoder_params`, `experiment`, `fingerprint`, `flags`, `name`, `output_shape`, `params`, `pretrained`, `signed_diff_enabled`, `total_params`

**⚠ Issues:** [none]

### `scripts/count_params_flops.py`

**Library used:** `fvcore` if installed; fallback manual parameter count.




## 15. Tests

```
tests/test_binary_metrics.py
tests/test_dataset_loading.py
```

### Test Coverage

| Test file | What it tests | Pass/Fail (if runnable) |
|-----------|--------------|------------------------|
| `tests/test_binary_metrics.py` | `BinaryMetrics` exact/confusion metric behavior. | See pytest output. |
| `tests/test_dataset_loading.py` | DSIFN/WHU dataset sample loading when roots are set. | See pytest output. |

**How to run tests:**
```bash
pytest tests/ -v
```



---


### Critical Issues (must fix before any public release)

| File | Line | Issue |
|------|------|-------|
| `src/models/backbone/mambavision_builder.py` | import-time | External sibling dependency required. |
| `configs/experiments/dsifn_full.yaml` | dataset.root | Hardcoded local path. |
| `configs/experiments/whu_full.yaml` | dataset.root | Hardcoded local path. |
| `scripts/infer.py` | preprocessing | No resize to configured image size. |

### Non-Critical Issues (nice to fix)

| File | Line | Issue |
|------|------|-------|
| `README.md` | install | `TODO_REPOSITORY_URL`. |
| `docs/EXPERIMENTS.md` | ablations | ablations marked TODO. |

### Hardcoded Paths

```
[NO OUTPUT]
```

| File | Line | Hardcoded value |
|------|------|----------------|
| `configs/experiments/dsifn_full.yaml` | dataset.root | `/storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN` |
| `configs/experiments/whu_full.yaml` | dataset.root | `/storage2/ChangeDetection/MV/Datasets/WHU-CD` |

### Unimplemented Features

| Feature | File | Evidence |
|---------|------|---------|
| Repo URL | `README.md` | `TODO_REPOSITORY_URL` |
| Ablation numeric results | `website/assets/tables/ablation_summary.csv` | header only |
| Exact environment versions | `requirements.txt` | lower bounds only |

---

## 17. Data Flow — End to End

```text
Disk
  └── Image pair + label
        │
        ▼
DSIFNCDDataset.__getitem__(idx) or WHUCDDataset.__getitem__(idx)
  ├── Load image_a/image_b with PIL.Image.open(...).convert("RGB")
  ├── Load mask with PIL.Image.open(...).convert("L")
  ├── Training transform when augment=True and split="train"
  ├── Normalize image tensors through norm_tensor
  └── Return dict with image_a, image_b, label, mask, id, name
        │
        ▼
DataLoader -> Trainer._step
        │
        ▼
DRBISiameseMambaCD.forward(img_a, img_b)
  ├── encoder(img_a), encoder(img_b)
  ├── D-RBI per scale -> diff, region, boundary
  ├── optional CRAMLiteBank.apply(region_feats)
  ├── AdaptiveRFDecoder -> P_c and optionally P_f
  └── returns (change_logits, aux_logits)
        │
        ▼
Loss computation
  ├── loss_fn(change_logits, label)
  ├── optional aux loss on aux_logits
  └── total = final + decoder.aux_weight * aux
        │
        ▼
optimizer.zero_grad / backward / grad clip / optimizer step / scheduler / EMA
        │
        ▼
Evaluator logs precision, recall, f1, iou, oa
```

---

## 18. Hyperparameter Reference

### Architecture Hyperparameters

| Parameter | Config key | Light | Base | Full | Notes |
|-----------|-----------|-------|------|------|-------|
| Backbone variant | `model.variant` | `tiny` | [NOT FOUND IN REPO] | `base` | active configs |
| D-RBI out channels | `difference.out_channels` | `128` | [NOT FOUND IN REPO] | `256` | active configs |
| D-RBI use depthwise | `difference.use_depthwise` | `true` | [NOT FOUND IN REPO] | `true` | active configs |
| Region gate min | `difference.region_gate_min` | `0.2` | [NOT FOUND IN REPO] | `0.2` | active configs |
| Region gate max | `difference.region_gate_max` | `0.8` | [NOT FOUND IN REPO] | `0.8` | active configs |
| Boundary gate min | `difference.boundary_gate_min` | `0.0` | [NOT FOUND IN REPO] | `0.0` | active configs |
| Boundary gate max | `difference.boundary_gate_max` | `0.4` | [NOT FOUND IN REPO] | `0.4` | active configs |
| ARF dilation rates | `decoder.dilation_rates` | `[1, 2, 4]` | [NOT FOUND IN REPO] | `[1, 2, 4, 8]` | active configs |
| Residual scale | `decoder.residual_scale` | `0.1` | [NOT FOUND IN REPO] | `0.1` | active configs |
| Aux loss weight | `decoder.aux_weight` | `0.4` | [NOT FOUND IN REPO] | `0.4` | active configs |

### Training Hyperparameters

| Parameter | Config key | LEVIR | WHU | DSIFN |
|-----------|-----------|-------|-----|-------|
| Seed | `experiment.seed` | [NOT FOUND IN REPO] | `42` | `42` |
| Epochs | `training.epochs` | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] | [NOT FOUND IN REPO] |
| Max iterations | `training.max_iterations` | [NOT FOUND IN REPO] | `50000` | `50000` |
| Batch size | `training.batch_size` | [NOT FOUND IN REPO] | `8` | `8` |
| Input size | `dataset.image_size` | [NOT FOUND IN REPO] | `256` | `256` |
| Optimizer | `training.optimizer` | [NOT FOUND IN REPO] | `adamw` | `adamw` |
| LR | `training.lr` | [NOT FOUND IN REPO] | `5.0e-5` | `5.0e-5` |
| Weight decay | `training.weight_decay` | [NOT FOUND IN REPO] | `0.01` | `0.01` |
| LR schedule | `training.scheduler` | [NOT FOUND IN REPO] | `cosine` | `cosine` |
| Warmup | `training.warmup_iterations` | [NOT FOUND IN REPO] | `2500` | `2500` |
| Gradient clip | `training.gradient_clip` | [NOT FOUND IN REPO] | `0.5` | `0.5` |

### Ablation Switches

| Switch | Config key | Default | What changes when false |
|--------|-----------|---------|------------------------|
| D-RBI enabled | `difference.enabled` | `true` | legacy abs-diff path |
| Use abs-diff | `difference.use_absdiff` | `true` | drops absolute difference |
| Use product | `difference.use_product` | `false` | drops product term |
| Use region gate | `difference.use_region_gate` | `true` | region = D |
| Use boundary gate | `difference.use_boundary_gate` | `true` | boundary = D |
| Use boundary residual | `decoder.use_boundary_residual` | `true` | no residual correction |

---


## Appendix — Raw Config Dump

### `configs/ablations/dsifn/a0_fpn_baseline.yaml`
```yaml
experiment:
  name: a0_fpn_baseline
  output_root: outputs/dsifn/a0_fpn_baseline
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: simple_cnn
  baseline_channels: [64, 128, 256, 512]
  variant: baseline
  decoder: baseline
  pretrained: false
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: false
  signed_diff:
    enabled: false
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: false
  boundary_refine:
    enabled: false

difference:
  enabled: false
  out_channels: 256
  use_absdiff: false
  use_signed_diff: false
  use_product: false
  pre_norm: false
  use_region_gate: false
  use_boundary_gate: false

decoder:
  type: baseline
  channels: 256
  dilation_rates: []
  use_boundary_residual: false
  residual_scale: 0.0
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

### `configs/ablations/dsifn/a1_mambavision_fpn.yaml`
```yaml
experiment:
  name: a1_mambavision_fpn
  output_root: outputs/dsifn/a1_mambavision_fpn
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: baseline
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: false
  signed_diff:
    enabled: false
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: false
  boundary_refine:
    enabled: false

difference:
  enabled: false
  out_channels: 256
  use_absdiff: false
  use_signed_diff: false
  use_product: false
  pre_norm: false
  use_region_gate: false
  use_boundary_gate: false

decoder:
  type: baseline
  channels: 256
  dilation_rates: []
  use_boundary_residual: false
  residual_scale: 0.0
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [1]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

### `configs/ablations/dsifn/a2_mambavision_drbi.yaml`
```yaml
experiment:
  name: a2_mambavision_drbi
  output_root: outputs/dsifn/a2_mambavision_drbi
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: baseline
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: false
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: false
  boundary_refine:
    enabled: false

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: false
  use_product: false
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: baseline
  channels: 256
  dilation_rates: []
  use_boundary_residual: false
  residual_scale: 0.0
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [1]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

### `configs/ablations/dsifn/a3_mambavision_drbi_signed.yaml`
```yaml
experiment:
  name: a3_mambavision_drbi_signed
  output_root: outputs/dsifn/a3_mambavision_drbi_signed
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: baseline
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: false
  boundary_refine:
    enabled: false

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: baseline
  channels: 256
  dilation_rates: []
  use_boundary_residual: false
  residual_scale: 0.0
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

### `configs/ablations/dsifn/a4_mambavision_drbi_arf.yaml`
```yaml
experiment:
  name: a4_mambavision_drbi_arf
  output_root: outputs/dsifn/a4_mambavision_drbi_arf
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: false

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: false
  residual_scale: 0.0
  use_depthwise: true
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

### `configs/ablations/dsifn/a5_mambavision_drbi_arf_boundary.yaml`
```yaml
experiment:
  name: a5_mambavision_drbi_arf_boundary
  output_root: outputs/dsifn/a5_mambavision_drbi_arf_boundary
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: false
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: true

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true
  aux_weight: 0.0

hardware:
  device: cuda
  gpu_ids: [1]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: false
    weight: 0.0
  boundary:
    enabled: false
    weight: 0.0

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

### `configs/ablations/dsifn/a6_full.yaml`
```yaml
experiment:
  name: a6_full
  output_root: outputs/dsifn/a6_full
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  augmentation_ops: [horizontal_flip, vertical_flip]
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1, 2]
    attention_type: spatial
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: true

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true
  aux_weight: 0.4

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25

ema:
  enabled: true
  decay: 0.999

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

### `configs/datasets/dsifn.yaml`
```yaml
# DSIFN-CD Dataset Configuration
dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN        # update to actual path
  train_split: train
  val_split: val
  test_split: test
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.2
  ignore_index: 255
  task_type: binary_change
  mode: binary
  binary: true
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 4

task: binary_cd

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]

checkpoint:
  selection_metric: F1
  mode: max
```

### `configs/datasets/whu.yaml`
```yaml
# WHU-CD Dataset Configuration
dataset:
  name: WHU-CD
  root: /storage2/ChangeDetection/MV/Datasets/WHU-CD          # update to actual path
  train_split: train
  val_split: val
  test_split: test
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.2
  ignore_index: 255
  task_type: binary_change
  mode: binary
  binary: true
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 4

task: binary_cd

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]

checkpoint:
  selection_metric: F1
  mode: max
```

### `configs/experiments/dsifn_full.yaml`
```yaml
experiment:
  name: dsifn_full
  output_root: outputs/dsifn/full
  seed: 42

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.2
  image_a_dir_candidates: [t1, T1, A, imageA, before, img1]
  image_b_dir_candidates: [t2, T2, B, imageB, after, img2]
  label_dir_candidates: [mask, GT, label, labels, OUT, change_map]

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1, 2]
    attention_type: spatial
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: true

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  product_scale: 0.25
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true
  aux_weight: 0.4

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01
  skip_nan_steps: true
  nan_diag_every: 50

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25
  log_mask_debug: true
  save_debug_outputs: false
  debug_output_root: debug/dsifn
  debug_max_samples: 30

ema:
  enabled: true
  decay: 0.999

resume:
  enabled: false
  checkpoint_path: null
  strict: true

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

### `configs/experiments/whu_full.yaml`
```yaml
experiment:
  name: whu_full
  output_root: outputs/whu/full
  seed: 42

dataset:
  name: WHU-CD
  root: /storage2/ChangeDetection/MV/Datasets/WHU-CD
  mode: binary
  task_type: binary_change
  image_size: 256
  batch_size: 8
  num_workers: 8
  augmentation: true
  val_ratio: 0.2

task: binary_cd

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1, 2]
    attention_type: spatial
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: true

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  product_scale: 0.25
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true
  aux_weight: 0.4

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01
  skip_nan_steps: true
  nan_diag_every: 50

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

eval:
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25
  log_mask_debug: true
  save_debug_outputs: false

ema:
  enabled: true
  decay: 0.999

resume:
  enabled: false
  checkpoint_path: null
  strict: true

checkpoint:
  selection_metric: F1
  monitor: f1
  mode: max
```

### `configs/global_config.yaml`
```yaml
# Runtime defaults for active binary change-detection experiments.

experiment:
  name: dsifn_full
  output_root: outputs/dsifn/full
  seed: 42

hardware:
  device: cuda
  gpu_ids: [0]
  mixed_precision: true

dataset:
  name: DSIFN-CD
  root: /storage2/ChangeDetection/MV/Datasets/DSIFN-CD/DSIFN
  mode: binary
  task_type: binary_change
  image_size: 256
  num_workers: 8
  pin_memory: true
  persistent_workers: true
  prefetch_factor: 4
  val_ratio: 0.2
  augmentation: true

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  num_classes: 1
  freeze_backbone: false
  drbi:
    enabled: true
  signed_diff:
    enabled: true
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1, 2]
    attention_type: spatial
  arf_fpn:
    enabled: true
  boundary_refine:
    enabled: true

difference:
  enabled: true
  type: region_boundary
  out_channels: 256
  use_depthwise: true
  gate_hidden_ratio: 0.25
  pre_norm: true
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  product_scale: 0.25
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4
  return_debug: false

decoder:
  type: adaptive_rf
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true
  aux_weight: 0.4

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01
  skip_nan_steps: true
  nan_diag_every: 50
  non_blocking_transfer: true

validation:
  batch_size: 4
  save_samples: true
  sample_count: 16
  split: val

evaluation:
  split: test
  threshold: 0.5
  threshold_sweep:
    enabled: true
    values: [0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.60]
  threshold_select_metric: F1_1
  use_ema: true
  inference_mode: patch
  crop_size: 256
  overlap: 0.25
  use_tta: false
  save_predictions: false

loss:
  type: bce_dice
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3

metrics:
  allowed: [Pre, Rec, F1, IoU, OA]
  average_mode: global

ema:
  enabled: true
  decay: 0.999
  store_on_cpu: true

resume:
  enabled: false
  checkpoint_path: null
  strict: true

checkpoint:
  path: null
  monitor: f1
  mode: max
  save_best_only: true

boundary_metrics:
  enabled: true
  boundary_width: 3
  tolerance: 2

debug:
  enabled: false
  output_root: debug
```

### `configs/models/mambarefinecd_full.yaml`
```yaml
# MambaRefineCD Full Model Configuration
# All modules enabled: signed diff, CRAM-lite, ARF-FPN, boundary refinement,
# boundary loss.

model:
  mode: dual
  backbone: mambavision
  variant: base
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  semantic_head_type: lightweight
  semantic_num_classes: 7
  num_classes: 1
  return_features: false
  freeze_backbone: false
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1, 2]
    attention_type: spatial

difference:
  enabled: true
  out_channels: 256
  use_depthwise: true
  use_absdiff: true
  use_signed_diff: true        # <── full: signed diff enabled
  use_product: false
  product_scale: 0.25
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4
  gate_hidden_ratio: 0.25

decoder:
  channels: 256
  dilation_rates: [1, 2, 4, 8]
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true

hardware:
  device: cuda
  mixed_precision: true

training:
  max_iterations: 50000
  batch_size: 8
  lr: 5.0e-5
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2500
  validate_every: 5000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3
```

### `configs/models/mambarefinecd_light.yaml`
```yaml
# MambaRefineCD Light Model Configuration
# Smaller channels and efficient settings for low-GPU-memory environments.

model:
  mode: dual
  backbone: mambavision
  variant: tiny              # lighter backbone
  decoder: adaptive_rf
  pretrained: true
  output_mode: binary
  enable_semantic_heads: false
  semantic_head_type: lightweight
  semantic_num_classes: 7
  num_classes: 1
  return_features: false
  freeze_backbone: false
  cram_lite:
    enabled: true
    alpha: 0.5
    apply_stages: [0, 1]     # fewer stages for efficiency
    attention_type: spatial

difference:
  enabled: true
  out_channels: 128          # narrower FPN channels
  use_depthwise: true
  use_absdiff: true
  use_signed_diff: true
  use_product: false
  product_scale: 0.25
  pre_norm: true
  use_region_gate: true
  use_boundary_gate: true
  region_gate_min: 0.2
  region_gate_max: 0.8
  boundary_gate_min: 0.0
  boundary_gate_max: 0.4
  gate_hidden_ratio: 0.25

decoder:
  channels: 128              # narrower decoder
  dilation_rates: [1, 2, 4]  # one fewer dilation rate
  use_boundary_residual: true
  residual_scale: 0.1
  use_depthwise: true

hardware:
  device: cuda
  mixed_precision: true

training:
  max_iterations: 40000
  batch_size: 16             # can fit larger batch with lighter model
  lr: 1.0e-4
  optimizer: adamw
  scheduler: cosine
  warmup_iterations: 2000
  validate_every: 4000
  log_every: 20
  gradient_clip: 0.5
  weight_decay: 0.01

loss:
  final:
    type: bce_dice
    bce_weight: 1.0
    dice_weight: 1.0
  coarse:
    enabled: true
    weight: 0.4
  boundary:
    enabled: true
    weight: 0.1
    type: bce_dice
    target_type: sobel
    edge_width: 3
```
